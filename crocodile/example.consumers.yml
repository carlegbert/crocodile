---
- name: app_name
  event_type: push # or other event as per github webhook API
  ref: refs/head/master # or other target
  action: /path/to/build/script.sh
  watchers:
    - notifications.get@sent.here
...
